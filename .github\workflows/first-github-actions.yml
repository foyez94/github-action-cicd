name: Run shell commands
on: [push]


jobs:
 run-shell-command:
  runs-on: ubuntu latest
  steps:
    - name: Echo a string 
      run: echo "Hello security groups"!! this is our first github action workflow file!!
